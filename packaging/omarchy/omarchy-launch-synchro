#!/bin/bash

# omarchy:summary=Launch Synchro
# omarchy:group=launch
#
# Reference launcher. Copy onto PATH (or cmake --install) so
# `{ omarchy = "synchro" }` works. Not installed into /usr/share/omarchy/.

exec setsid uwsm-app -- synchro --new-window
